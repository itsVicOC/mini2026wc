.small-hero .hero-title {
  font-size: 40rpx;
}

.scorer-list {
  margin-top: 24rpx;
  overflow: hidden;
}

.scorer-row {
  display: grid;
  grid-template-columns: 64rpx minmax(0, 1fr) 90rpx 90rpx;
  align-items: center;
  min-height: 104rpx;
  padding: 0 20rpx;
  border-bottom: 1rpx solid #eef2ef;
}

.scorer-row:last-child {
  border-bottom: 0;
}

.rank {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 44rpx;
  height: 44rpx;
  border-radius: 50%;
  background: #edf2ef;
  color: #244931;
  font-weight: 900;
}

.rank.top {
  background: #f4e5bd;
  color: #8a5a00;
}

.player {
  min-width: 0;
}

.player-name {
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
  font-size: 28rpx;
  font-weight: 800;
}

.player-team {
  margin-top: 4rpx;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
  font-size: 23rpx;
}

.goal-box,
.assist-box {
  text-align: center;
}

.goal-value {
  color: #0b7a3b;
  font-size: 34rpx;
  font-weight: 900;
}

.assist-value {
  color: #27342d;
  font-size: 30rpx;
  font-weight: 800;
}

.goal-label {
  margin-top: 2rpx;
  color: #77837c;
  font-size: 20rpx;
}

